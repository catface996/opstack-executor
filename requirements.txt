# =============================================================================
# Core dependencies for hierarchical agent system
# =============================================================================

# Agent Framework
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# AWS Bedrock support
boto3>=1.34.0

# HTTP Server dependencies
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# API Documentation
flasgger>=0.9.7

# Database
SQLAlchemy>=2.0.0
alembic>=1.13.0

# MySQL driver (using pure Python driver for easier Docker builds)
pymysql>=1.1.0
cryptography>=42.0.0  # Required for MySQL 8.0 caching_sha2_password

# PostgreSQL driver (optional, uncomment if using PostgreSQL)
# psycopg2-binary>=2.9.0

# Request validation
pydantic>=2.5.0

# Redis for event streaming
redis>=5.0.0
